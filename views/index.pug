extends layout

block content
  h1= title
  p Welcome to Index.pug this is Rudra Teja Annangi
  input(type ="button", class="btn btn-info" value = "Press Me" onClick="update()")
  <br>
  div(id = "target") Display

  script.
      var client_count=0
      function update(){
        client_count += 1;
        document.getElementById('target').innerHTML = "Value now : " + client_count
      };
